alias bdf='df -h'
alias j='jobs -l'
alias mv='mv -i'
alias rm='rm -i'
alias cp='cp -i'
alias h=history
alias lports='NETSTAT.EXE -an | grep LISTENING'
alias ..='cd ..'         # Go up one directory
alias ...='cd ../..'     # Go up two directories
alias ....='cd ../../..' # Go up three directories
